import { bem } from '../utils/classnames';

const [graphBlock] = bem('graph-layer');

const StatisticGraph = () => {
	return <div className={`${graphBlock} common-layer`}>wait</div>;
};

export default StatisticGraph;

