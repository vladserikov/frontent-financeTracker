import { bem } from '../utils/classnames';

export const [defaultButtonBlock, , defaultButtonModification] =
	bem('default-button');

export const [defaultInputWrapper, defaultInputElement] = bem('input-block');
